package fr.blazanome.routeplanner.controller.state;

/**
 * Default state after a map has been loaded
 * This state doesn't overwrite any method as they are shared with the other states
 */
public class MapLoadedState implements State {
}
